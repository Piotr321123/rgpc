sudo apt install cmake
sudo apt install g++
sudo apt install -y build-essential autoconf libtool pkg-config
sudo apt install curl
sudo apt install git
git clone --recurse-submodules -b v1.33.2 https://github.com/grpc/grpc
cd grpc

mkdir -p cmake/build
pushd cmake/build
cmake -DgRPC_INSTALL=ON \
      -DgRPC_BUILD_TESTS=OFF \
      -DCMAKE_INSTALL_PREFIX=$MY_INSTALL_DIR \
      ../..
make -j
make install
popd

make 
make install

//mam nadzieję, że to wszystkie kroki. Niestety, kiedy próbowałem postawić maszynę od zera w czasie wykonywania kroku z linii 15, zacięło mi się ubuntu i 
//nie byłem w stanie dokończyć tego przed 24 

